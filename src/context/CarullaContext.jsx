// Importar la función createContext desde la biblioteca de React
import { createContext } from "react";

// Crear y exportar el contexto PokemonContext
// Este contexto se utilizará para compartir el estado relacionado con los Pokémon en toda la aplicación
export const CarullaContext = createContext();
